spring:
  main:
    allow-bean-definition-overriding: true
  security:
    admin:
      username: admin
      password: admin

edc:
  x-api-key: test
  mgmt-base-url: test
  protocol-base-url: test

fh:
  catalog:
    repo:
      url: url
    sparql:
      url: url
    uri-resource-base: https://example.com/
    secret-key: secret

sd-creation-wizard-api:
  base-url: test

participant-id: "did:web:test.eu"

daps-server:
  base-url: http://localhost:4567

s3:
  bucket-storage-region: "something"
  bucket-name: "something"
  bucket-top-level-folder: "something/"

prefill-fields:
  data-product:
    json-file-path: "src/test/resources/prefillFieldsDataProduct.json"

version.no: "1.0.0"

version.date: "2024-12-31"
