spring:
  profiles:
    active: default
  application:
    name: edc-orchestrator

management:
  endpoints:
    enabled-by-default: false
    web:
      base-path: "/"
  endpoint:
    health:
      enabled: true

edc:
  x-api-key: 5678
  mgmt-base-url: http://localhost:19193/management